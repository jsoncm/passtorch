{
  "name": "passtorch",
  "version": "0.1.0",
  "description": "Local-first cross-agent memory daemon and MCP server",
  "type": "module",
  "bin": {
    "passtorch": "dist/bin.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch"
  },
  "keywords": [
    "mcp",
    "agent",
    "memory",
    "daemon"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "env-paths": "^4.0.0"
  }
}
